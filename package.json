{
  "name": "useless-calculator-gerardo-dev",
  "version": "1.0.0",
  "description": "una calculadora que nadie usara test",
  "main": "dist-node/index.js",
  "browser":"dist-browser/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-browser": "tsc src/index.ts --outDir dist-browser --module ES6",
    "build-node": "tsc src/index.ts --outDir dist-node",
    "build": "npm run build-browser & npm run build-node"
  },
  "keywords": ["useless"],
  "author": "gerardo_dev test",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
